{% extends 'base.html.twig' %}

{% block title %}Ordonnance index{% endblock %}

{% block body %}
    <h1>Ordonnance index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>IDConsultation</th>
            <th>Title</th>
            <th>Date</th>
            <th>Traitement</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for ordonnance in ordonnances %}
            <tr>
                <td>{{ ordonnance.id }}</td>
                <td>{{ ordonnance.refToConsultation.id }}</td>
                <td>{{ ordonnance.titre }}</td>
                <td>{{ ordonnance.Date ? ordonnance.Date|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ ordonnance.Traitement }}</td>
                <td>
                    <a href="{{ path('ordonnance_show', {'id': ordonnance.id}) }}">show</a>
                    <a href="{{ path('ordonnance_edit', {'id': ordonnance.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="10">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ordonnance_new', {'id_consultation': id_con}) }}">Create new</a>
{% endblock %}
